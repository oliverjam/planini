const ingredient = require('./ingredient');
const allIngredients = require('./allIngredients');
const me = require('./me');
const recipe = require('./recipe');

module.exports = { ingredient, allIngredients, me, recipe };
